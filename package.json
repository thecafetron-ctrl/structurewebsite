{
  "name": "structure-static-site",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "dev": "node scripts/serve-local.js",
    "build": "echo 'static site — nothing to build'",
    "indexnow": "bash scripts/submit-indexnow.sh"
  }
}
